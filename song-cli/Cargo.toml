[package]
name = "song-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.1.9", features = ["derive"] }
songs = { path = "../songs" }
chrono = "0.4"
csv = "1.1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.0"
predicates = "2.1"